import Portfolio, {
  loader as portfolioLoader,
  meta as portfolioMeta,
} from "~/components/portfolio/Portfolio";

export const loader = portfolioLoader;
export const meta = portfolioMeta;

export default Portfolio;
